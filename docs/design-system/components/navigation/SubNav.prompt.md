The in-page back row that sits above the main card on detail screens.

```jsx
<SubNav onBack={goBack} right={<DropdownMenu trigger={<Icon name="more-horizontal" size={24}/>}>…</DropdownMenu>} />
```

Both glyphs are 24px indigo. On the compose screen the same pattern is pinned inside the fixed navbar with the "Post hoojah" pill on the right instead.
