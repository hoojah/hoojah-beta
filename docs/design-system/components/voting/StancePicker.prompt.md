Picks the stance a reply is posted as — three 48px stance buttons spread evenly under a small grey prompt.

```jsx
<StancePicker value={stance} onChange={setStance} />
```

Appears in the compose screen only when replying to a parent hoojah, and defaults to the replier's existing vote on that parent.
