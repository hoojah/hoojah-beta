A 16px native checkbox with a 14px grey label — used for "Remember me" and the private-account toggle.

```jsx
<Checkbox label="Private account — approve followers before they see your hoojahs" checked={priv} onChange={toggle} />
```

There is no custom switch component in Hoojah; a real checkbox is the toggle.
