The validation summary at the top of a form — the only place red appears in Hoojah.

```jsx
<FormError messages={["Username has already been taken"]} />
```

Errors are summarised at the top of the form, not annotated per field.
