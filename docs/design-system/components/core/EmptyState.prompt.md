A one-line empty state — faint grey, one Lucide glyph, one plain sentence. Never an illustration, never an emoji.

```jsx
<EmptyState icon="message-circle">No response yet</EmptyState>
<EmptyState icon="bell">You have no notifications</EmptyState>
<EmptyState icon={null} tone="muted" align="center">Waiting for @mayaz…</EmptyState>
```

Real copy from the product: "No response yet", "You have no notifications", "You haven't posted any hoojahs yet", "Nothing trending yet.", "No debates yet — challenge a response to start one."
