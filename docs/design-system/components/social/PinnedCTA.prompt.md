The signup card pinned above the feed for logged-out visitors — the one place Hoojah uses purple as a fill.

```jsx
<PinnedCTA onSignUp={goSignUp} />
```

Copy is verbatim: "Create a Hoojah account / to secure your @username now!" and "Click here to sign up!". Colours are fixed (disagree purple card, agree orange button); do not retint it.
