The row used in the followers and following lists.

```jsx
<UserRow user={u} onClick={openProfile} />
```

40px avatar, medium name, faint light-grey @handle; hovers to `--color-gray-50`. Rows sit directly on white with a bottom border, not on separate cards.
