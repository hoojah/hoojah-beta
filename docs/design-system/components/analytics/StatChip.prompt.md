A headline stat on the owner-only dashboard.

```jsx
<StatChip label="Votes received" value={128} />
<StatChip label="Arguments received" value={41} />
```

Only two exist in the product; they sit side by side with a 12px gap. Aggregates only — Hoojah never shows per-voter data.
