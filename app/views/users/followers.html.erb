<%# Public list of users who follow @user. %>
<%= render "shared/navbar" %>

<main class="max-w-xl mx-auto px-4 py-6">
  <h1 class="text-lg font-medium mb-3">
    People following @<%= @user.username %>
  </h1>

  <div class="flex flex-col shadow">
    <% if @users.any? %>
      <%= render partial: "user_row", collection: @users, as: :user %>
    <% else %>
      <div class="flex items-center text-sm text-light-grey fill-light-grey p-3 bg-white">
        <%= lucide_icon("users", class: "w-4 h-4") %>
        <span class="ml-1">No followers yet</span>
      </div>
    <% end %>
  </div>
</main>
