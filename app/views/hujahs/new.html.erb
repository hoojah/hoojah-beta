<%# Compose / respond screen — faithful port of the legacy SPA `Hujah/form.js`.
    Top-level compose when @parent is nil; a stance-tagged reply when @parent is set. %>
<%= render "hujahs/compose_form", hujah: @hujah, parent: @parent %>
